httpx
pyyaml
playwright
torch
faster-whisper
click
jinja2
pillow

# LangGraph drives the pipeline itself (msf/graph/video_graph.py and Studio research-to-script workflow).
langgraph>=1,<2
# Public search results for native local research; no provider API key is required.
ddgs>=9,<10

# Audio: numpy/scipy synthesise the SFX and music beds, soundfile reads and
# writes reference wavs, and voice_prep measures them.
numpy
scipy
soundfile

# Control panel (msf/panel/server.py). Verified working versions on this machine:
# fastapi 0.133.1, uvicorn 0.41.0, pydantic 2.13.4 on python 3.11.15.
# pydantic 2.x is REQUIRED — the request models use Field(...) v2 semantics.
fastapi
uvicorn
pydantic>=2

# Local stdio MCP adapter for agent clients (msf/studio/mcp_server.py).
mcp>=1,<2

# Tests. This repo uses unittest classes but they are run with pytest.
pytest
